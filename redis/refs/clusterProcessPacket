<def f='codebrowser/src/cluster.c' l='1607' ll='2050' type='int clusterProcessPacket(clusterLink * link)'/>
<use f='codebrowser/src/cluster.c' l='2139' u='c' c='clusterReadHandler'/>
<doc f='codebrowser/src/cluster.c' l='1598'>/* When this function is called, there is a packet to process starting
 * at node-&gt;rcvbuf. Releasing the buffer is up to the caller, so this
 * function should just handle the higher level stuff of processing the
 * packet, modifying the cluster state if needed.
 *
 * The function returns 1 if the link is still valid after the packet
 * was processed, otherwise 0 if the link was freed since the packet
 * processing lead to some inconsistency error (for instance a PONG
 * received from the wrong sender ID). */</doc>
