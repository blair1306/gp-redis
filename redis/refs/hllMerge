<def f='codebrowser/src/hyperloglog.c' l='1037' ll='1076' type='int hllMerge(uint8_t * max, robj * hll)'/>
<use f='codebrowser/src/hyperloglog.c' l='1215' u='c' c='pfcountCommand'/>
<use f='codebrowser/src/hyperloglog.c' l='1296' u='c' c='pfmergeCommand'/>
<doc f='codebrowser/src/hyperloglog.c' l='1029'>/* Merge by computing MAX(registers[i],hll[i]) the HyperLogLog &apos;hll&apos;
 * with an array of uint8_t HLL_REGISTERS registers pointed by &apos;max&apos;.
 *
 * The hll object must be already validated via isHLLObjectOrReply()
 * or in some other way.
 *
 * If the HyperLogLog is sparse and is found to be invalid, C_ERR
 * is returned, otherwise the function always succeeds. */</doc>
