<def f='codebrowser/src/t_hash.c' l='109' ll='125' type='int hashTypeGetValue(robj * o, sds field, unsigned char ** vstr, unsigned int * vlen, long long * vll)'/>
<use f='codebrowser/src/t_hash.c' l='136' u='c' c='hashTypeGetValueObject'/>
<use f='codebrowser/src/t_hash.c' l='568' u='c' c='hincrbyCommand'/>
<use f='codebrowser/src/t_hash.c' l='604' u='c' c='hincrbyfloatCommand'/>
<doc f='codebrowser/src/t_hash.c' l='100'>/* Higher level function of hashTypeGet*() that returns the hash value
 * associated with the specified field. If the field is found C_OK
 * is returned, otherwise C_ERR. The returned object is returned by
 * reference in either *vstr and *vlen if it&apos;s returned in string form,
 * or stored in *vll if it&apos;s returned as a number.
 *
 * If *vll is populated *vstr is set to NULL, so the caller
 * can always check the function return by checking the return value
 * for C_OK and checking if vll (or vstr) is NULL. */</doc>
