<def f='codebrowser/src/cluster.c' l='742' ll='756' type='void clusterNodeCleanupFailureReports(clusterNode * node)'/>
<use f='codebrowser/src/cluster.c' l='785' u='c' c='clusterNodeDelFailureReport'/>
<use f='codebrowser/src/cluster.c' l='793' u='c' c='clusterNodeFailureReportsCount'/>
<doc f='codebrowser/src/cluster.c' l='737'>/* Remove failure reports that are too old, where too old means reasonably
 * older than the global node timeout. Note that anyway for a node to be
 * flagged as FAIL we need to have a local PFAIL state that is at least
 * older than the global node timeout, so we don&apos;t just trust the number
 * of failure reports from other nodes. */</doc>
