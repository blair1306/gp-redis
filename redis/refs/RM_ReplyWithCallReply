<def f='codebrowser/src/module.c' l='1138' ll='1144' type='int RM_ReplyWithCallReply(RedisModuleCtx * ctx, RedisModuleCallReply * reply)'/>
<use f='codebrowser/src/module.c' l='3912' macro='1' u='r'/>
<doc f='codebrowser/src/module.c' l='1132'>/* Reply exactly what a Redis command returned us with RedisModule_Call().
 * This function is useful when we use RedisModule_Call() in order to
 * execute some command, as we want to reply to the client exactly the
 * same reply we obtained by the command.
 *
 * The function always returns REDISMODULE_OK. */</doc>
