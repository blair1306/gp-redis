<def f='codebrowser/src/cluster.c' l='927' ll='938' type='void clusterRenameNode(clusterNode * node, char * newname)'/>
<use f='codebrowser/src/cluster.c' l='1782' u='c' c='clusterProcessPacket'/>
<doc f='codebrowser/src/cluster.c' l='923'>/* This is only used after the handshake. When we connect a given IP/PORT
 * as a result of CLUSTER MEET we don&apos;t have the node name yet, so we
 * pick a random one, and will fix it when we receive the PONG request using
 * this function. */</doc>
